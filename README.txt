This is merely a backend server that connects to a SQL Server Database.

Configuration for database connection can be done through the connection string property in appsetting.json

There is no UI attached to this as it was to merely test the functionality of the Dapper ORM.

Postman was used to test the api.